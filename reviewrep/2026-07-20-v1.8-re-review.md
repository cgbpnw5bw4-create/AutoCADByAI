# Claude Code 审查报告 — V1.8 二次审查

**审查日期**：2026-07-20
**审查者**：Claude Code（只读代码审查 Agent）
**审查对象**：Codex 针对 `2026-07-20-v1.8-review.md` 的修复，HEAD = `ec3bd1a`，含未提交工作区改动
**审查模式**：深度可靠性审查（复核 MEDIUM-001 + LOW-003 + 关联项）
**声明**：全程只读。未修改主项目任何文件；未执行任何 git 写操作（无 add/commit/reset/checkout/clean/stash）；未运行 build / test / self-check。结论由构建产物符号 + self-check 运行元数据 + 源码静态分析交叉得出。

---

## 1. 审查结论

**PASS — 可以进入下一阶段。**

上轮唯一的 MEDIUM（MEDIUM-001）及其关联的 LOW-003 均已正确闭环。无 BLOCKER、无 HIGH。self-check `final_status:Passed`，`source_revision` 匹配当前 HEAD，dll 今日新构建、新符号编译入。

---

## 2. 逐条验证

### ✅ MEDIUM-001（`large_switch` 字段字符串匹配）— 已修复

`part_family_builders_do_not_use_large_switch`（`PlatformSelfCheckRunner.cs:849-854`）从 grep `switch (partType` 改为**反射 + 结构一致性**：

```csharp
var partFamilyBuildersDoNotUseLargeSwitch =
    builderRegistryType is not null &&
    builderRegistryType.GetMethod("Register", ...) is not null &&
    builderRegistryType.GetMethod("TryGetBuilder", ...) is not null &&
    definitionPartTypes.Count == 3 &&
    definitionPartTypes.SetEquals(builderPartTypes);
```

检查的是编译后真实类型、方法签名、以及 Definition registry 与 Builder registry 的 part_type 集合一致性——真实结构证据，不再有任何 `switch (partType` 字符串。self-check 中该字段已无字符串匹配残留（全文件确认）。

### ✅ MEDIUM-001 根治（防复发元测试通用化）— 已修复（区间约束）

`V17SelfCheckUsesBehavioralEvidenceInsteadOfSourceTextMatches` 新增：

```csharp
Assert.DoesNotContain("smokeRunnerText", source, ...);
var v18Start = source.IndexOf("RunV18PartFamilyChecksAsync", ...);
var v18End = source.IndexOf("private static bool TryWriteJsonReport", v18Start, ...);
Assert.DoesNotContain("File.ReadAllText", source[v18Start..v18End], ...);
```

关键进步：不再逐变量名钉死，而是**断言整个 V1.8 self-check 检查区间内不出现 `File.ReadAllText`**——禁止的是"读源码文本当证据"这个手法本身。任何新的 V1.8 字符串字段都会被此约束拦下。这正是我上轮建议的方向。

### ✅ LOW-003（旧 `smokeRunnerText.Contains` 残留）— 已修复

新增 `src/WorkerContracts/SolidWorksApiRepairPolicy.cs`（纯策略类，注释明写 "retry capability is proven by behavior instead of source-text matching"）：

```csharp
public const int MaxRepairAttempts = 1;
public static bool CanAttempt(string? failureStage, bool repairAlreadyAttempted) =>
    string.Equals(failureStage, "cut_holes_failed", ...) && !repairAlreadyAttempted;
```

接入验证（行为无回归）：
- `SolidWorksDiagnosticRunner.cs`：`MaxRepairAttempts = SolidWorksApiRepairPolicy.MaxRepairAttempts`，判定改为 `SolidWorksApiRepairPolicy.CanAttempt(...)`。原逻辑 `FailureStage=="cut_holes_failed" && !ApiRepairAttempted` 与 `CanAttempt` **等价**。
- self-check（`1326-1329`）：从 grep 源码改为消费真实策略值 `SolidWorksApiRepairPolicy.MaxRepairAttempts == 1 && CanAttempt(...)==... `。
- `SolidWorksModuleSkeletonTests.cs`：新增真实行为断言（`MaxRepairAttempts==1`、`CanAttempt("cut_holes_failed", false)==true`、`...true==false`、`save_sldprt_failed==false`）。

### 交叉验证
- self-check：schema `1.8`、`final_status:Passed`、`source_revision = ec3bd1a-dirty` **匹配当前 HEAD**、mtime 今日 14:08、全部 V1.8 字段 true。
- 编译：`PlatformCore.dll` / `WorkerContracts.dll` / `PlatformSelfCheck.Tests.dll` 今日 14:01 新构建；`SolidWorksApiRepairPolicy` / `V18PartFamilyTests` 编译入 dll。
- V1.8 核心结论（零件族 registry 字典分发、假成功双层 fail-closed gate、三来源授权未回退）在上轮已确认，本轮未退化。

---

## 3. 剩余项（不阻塞下一阶段）

### LOW-001（延续，MEDIUM 观察）：更老的源码字符串检查残留未纳入元测试锁

`PlatformSelfCheckRunner.cs:1305-1319` 仍有一批 `apiFailureAnalyzerText.Contains(...)` / `apiEvidenceCollectorText.Contains(...)`（用源码 `.cs` 文本证明 `SolidWorksApiFailureAnalyzer` / `SolidWorksApiEvidenceCollector` 存在及含某些 API 描述字符串）。

**定性**：这是 **HEAD 已有的历史残留**（`git show HEAD` 确认存在），**非本轮回归**。本轮新增的元测试锁是 **V1.8 区间**的（`RunV18PartFamilyChecksAsync`→`TryWriteJsonReport`），不覆盖这批更老的区块。严重性 LOW-MEDIUM：这些是诊断器/收集器**存在性检查**、非安全属性，且同族的 `solidWorksApiEvidenceReportSchemaExists` 已用反射（`typeof(ApiEvidenceReport).GetProperties()`）——证明反射替代路径可行。

**建议（下一阶段顺手，非阻塞）**：把这批 `apiFailureAnalyzerText.Contains("SolidWorksApiFailureAnalyzer")` 改为 `Type.GetType("...SolidWorksApiFailureAnalyzer")` 反射（参照 V1.7 的 `realCadE2eCanInvokeRealWorker`）；并把元测试锁从"V1.8 区间"扩展为"全 self-check 文件禁止用 `.cs` 源码文本当代码能力证据"（`.md` 文档合规检查如 `governanceText.Contains` 应豁免——那是验证文档内容本身，非代替行为证据）。

### 其余延续项（均账本登记）
- **LOW（dotnet test 落盘证据）**：第四轮延续；self-check 已真实执行行为验证 + provenance 匹配，运行时证据链不空白。建议尽早落一份 TRX。
- ARCH-B `InternalRoute` 动态化；真实 CAD 执行前 HumanApproval 门；flange/shaft 真实 API smoke（保持 dry-run-only）。

---

## 4. 下一阶段建议

**可以进入下一阶段。**

上轮 PASS WITH COMMENTS 的唯一 MEDIUM 已闭环，修复方式质量高（反射结构证据 + 手法级元测试锁 + 可执行策略类），无功能回归，self-check 与编译状态健康。

下一阶段优先：
1. **产出一次真实成功的 E2E 证据**（延续 V1.7）——仓库仍无一份 `final_status:Passed` 的真实 SolidWorks 产物；机器与 plate-only 授权 gate 已就位，在真机上刷出一次 `Deliverable` 才算闭环。
2. **把元测试锁从 V1.8 区间扩展为全文件通用**，顺手把 `apiFailureAnalyzerText` 那批改反射（LOW-001）——一次性根治这个我追踪多轮的模式。
3. flange/shaft 真实 API smoke（勿越界，先 evidence 后 dry-run 解锁）。
4. ARCH-B 动态 `InternalRoute`。

---

*本报告由 Claude Code 生成，仅写入 `BIGPROJECT/reviewrep/`，未修改主项目任何文件，未执行任何 git 写操作，未运行 build / test / self-check。*
